{
  "name": "@pgsty/sow-edge-contract",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
	"build": "node build.mjs",
	"test": "node build.mjs --check && node --check dist/cloudflare-worker.mjs && node --check dist/cloudflare-origin-worker.mjs && node --check dist/edgeone.js && node --test test/*.test.mjs"
  }
}
