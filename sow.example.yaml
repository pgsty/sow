schema: sow/v3
architectures:
  - x86_64
  - aarch64

repos:
  pgsql:
    dists:
      el9:
        format: rpm
        exclude:
          - kind: [debug]
      noble:
        format: deb

# Publication targets are optional. Add a validated filesystem or R2 target
# only when this workspace is ready to publish.
targets: {}
