# SOW V2 P0 Plain real-client evidence — 2026-08-01

Status: **PASS** for the scoped P0 Plain real-client proof.

This is a fresh run of `test/poc/plain-clients/run.sh`. The proof built the
current checkout's real `cmd/sow` binary and invoked its public
`sow create -j 2 --json` command for RPM-only, DEB-only, and mixed flat
directories. No test helper called `plain.Create` directly.

The report proves local source behavior and Linux package-client compatibility.
It is not signing, deployment, upload, publication, or public-availability
evidence.

## Reproduction identity

- Command: `test/poc/plain-clients/run.sh`
- Exit status: `0`
- UTC window: 2026-08-01 05:31:48–05:32:02
- Local window: 2026-08-01 13:31:48–13:32:02 CST
- Git HEAD: `50f183cc8125d09199dcdf38eec4fd86eeb338bf`
- Go: `go1.26.5 darwin/arm64`
- Docker server: `29.4.1`
- Retained lab: `/Users/vonng/repo/sow-v2-plain-clients.IC4VHX`
- Retained full log: `/Users/vonng/repo/sow-v2-plain-clients.IC4VHX/run.log`
- Full log size: 19,134 bytes, 389 lines
- Full log SHA-256: `28525cd5c853dc564eef01b8a261bebc1b7ece8415d25d76cb1f3af2f6740bad`

The relevant dirty-source fingerprint is
`cc1751f6904760d258c48eca1cf647abc4ea3fb367dca9ede758b98f5a625703`.
It is the SHA-256 of the sorted SHA-256 listing of all Go files under
`cmd/sow`, `internal/v2cli`, `internal/v2/plain`, `internal/aptrepo`, and
`internal/yumrepo`. This captures the uncommitted CLI and implementation used
by the run; Git HEAD alone does not.

The script built the executable with:

```text
go build -trimpath -o /Users/vonng/repo/sow-v2-plain-clients.IC4VHX/bin/sow ./cmd/sow
```

- Binary SHA-256: `bcb91f9eff5ed45ee059f3a41c372bca0cb251e3d4fbfe16b1712c73d5ac6b68`
- Version output: `sow 0.2.0-dev darwin/arm64 go1.26.5`

## Client environments

| Purpose | Cached image | Immutable image ID | Client |
| --- | --- | --- | --- |
| RPM build/client | `pgsty/el9:build` | `sha256:7ae1657642a32d84a5b2bf28403ad7e01b62d5279d253cc63bf36a358bed5292` | DNF/YUM 4.14.0, RPM 4.16.1.3 |
| DEB build/client | `pgsty/u24:latest` | `sha256:190f49d7960622e40884b17f518e46866aab8fcd8c0ac06e1de0e88ac69191cb` | APT 2.8.3 amd64 |

Both images ran with `--pull never --platform linux/amd64`.

## Fixtures and generated repository results

The script generated and inspected four installable packages:

| Fixture | Architecture | SHA-256 |
| --- | --- | --- |
| `sow-plain-native-1.0.0-1.el9.x86_64.rpm` | RPM x86_64 | `b95866807b42dad449c873e28e4d80022fcb6efc1dc6496fe5e89a6d94b20dc5` |
| `sow-plain-neutral-1.0.0-1.el9.noarch.rpm` | RPM noarch | `6fd19a731534a758c1a4397ff6a31ec1bc1a02ab8eb83077c116b4f08f121640` |
| `sow-plain-deb-native_1.0.0-1_amd64.deb` | DEB amd64 | `fbe09542b9afda44e6849af07a4980f23d8c8a2a3fe4b3835939136787a75534` |
| `sow-plain-deb-neutral_1.0.0-1_all.deb` | DEB all | `80adccd4cbd868ebb472df78d82baef61e9acf9af0658f2fa7cc399c0f1b1fad` |

Actual public-command outputs were retained separately:

| Directory | Result | JSON SHA-256 |
| --- | --- | --- |
| RPM-only | `rpm=2`, `deb=0`, `marker=false` | `0ae4eccae1e08e0c9cfe7d293dbfbb2a8a8b37a31a2a21be2dbd599a035238ce` |
| DEB-only | `rpm=0`, `deb=2`, `marker=false` | `68a361afd7bbfba2aca7718f7eddaf942cb9dd04b1167436289478b38d8c5607` |
| Mixed | `rpm=2`, `deb=2`, `marker=false` | `f33c3c22f0b90bc884f9516f591873a4d6828b9ff666c15e7b6c22cfcf5168a9` |

For all three responses, the harness parsed JSON and asserted
`schema="sow.cli/v1"`, `command="create"`, `ok=true`, null repository and
operation fields, an empty errors array, the expected counts, and
`marker=false` and an empty `removed` array.

No `repo_complete`, SQLite/database file, or modulemd output existed in any
default Plain repository.

Metadata assertions passed:

- RPM-only and mixed primary metadata locations were exactly the two RPM
  basenames, with no directory prefix.
- DEB-only and mixed `Filename` fields were exactly
  `./sow-plain-deb-native_1.0.0-1_amd64.deb` and
  `./sow-plain-deb-neutral_1.0.0-1_all.deb`.
- `Packages.gz` decompressed byte-for-byte to `Packages`.

## Real-client outcomes

RPM-only repository:

- `dnf makecache`: passed (`Metadata cache created`).
- `yum makecache`: passed (`Metadata cache created`).
- `dnf repoquery --location`: returned `file:///repo/<basename>` for the native
  and noarch packages.
- `dnf install --downloadonly`: downloaded exactly two RPMs.
- `dnf install`: installed both packages; `rpm -q` and installed payload-file
  assertions passed.

DEB-only repository:

- `apt-get update`: passed and read the flat `Packages` index.
- `apt-cache policy`: located both packages at `file:/repo ./ Packages`.
- `apt-get download`: downloaded exactly two DEBs.
- `apt-get --download-only install`: passed.
- `apt-get install`: installed both packages; `dpkg-query` and installed
  payload-file assertions passed.

Mixed repository:

- DNF refreshed and queried both RPM packages while DEBs were present at the
  same top level.
- APT refreshed and queried both DEB packages while RPMs were present at the
  same top level.

## Safety and evidence boundary

All package creation, repository mutation, and client tests occurred beneath
the retained lab path. The script accepts only a real path matching
`/Users/vonng/repo/sow-v2-plain-clients.*` and rejects the production prefix.
`/Users/vonng/pgsty/repo` was not used as an input, mount, or target. Nothing
was signed, uploaded, or published.

The retained evidence above comes from one fresh, exit-zero run after the
current encoder and recovery implementation were frozen. Earlier harness
development runs are not used for any package-client claim.
