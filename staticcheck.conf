# SOW preserves established protocol spellings and vendor wire names (for
# example KeysById and RequestUrl), and splits large internal packages across
# many focused files without package boilerplate on each file. Keep all
# correctness checks enabled and exempt only these presentation conventions.
checks = ["all", "-ST1000", "-ST1003", "-ST1005", "-ST1020"]
